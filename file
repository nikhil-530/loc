1111111111111111111
void main(){
    int a =10;
    String knxos ="jbbs";

    print("Hello $knxos");
    for(int i=0;i< 5;i++){
        print("hi");
    }
    int b = add(2,3);
    print("$b");
    Animal myanimal = Animal("Puppy");
    myanimal.hello();

}
int add(int a, int b){
    return a + b ;
}
class Animal {
    String name;
    Animal(this.name);
    hello(){
        print("Hello $name");
    }
}





222222222222222222

import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Flutter Widgets & Layouts'),
          backgroundColor: Colors.deepPurple,
        ),

        // Body of the app
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              // ===== Text Widget =====
              Text(
                'Welcome to Flutter!',
                style: TextStyle(
                  fontSize: 24,
                  color: Colors.blue,
                  fontWeight: FontWeight.bold,
                ),
              ),
              SizedBox(height: 20),

              // ===== Image Widget =====
              Image.network(
                'https://flutter.dev/images/flutter-logo-sharing.png',
                width: 120,
              ),
              SizedBox(height: 20),

              // ===== Container Widget =====
              Container(
                padding: EdgeInsets.all(15),
                color: Colors.amber,
                child: Text(
                  'This is a Container Widget',
                  style: TextStyle(fontSize: 18),
                ),
              ),
              SizedBox(height: 30),

              // ===== Row Widget (Horizontal layout) =====
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Icon(Icons.favorite, color: Colors.red, size: 40),
                  SizedBox(width: 10),
                  Icon(Icons.star, color: Colors.orange, size: 40),
                  SizedBox(width: 10),
                  Icon(Icons.thumb_up, color: Colors.blue, size: 40),
                ],
              ),
              SizedBox(height: 30),

              // ===== Stack Widget (Overlapping layout) =====
              Stack(
                alignment: Alignment.center,
                children: [
                  Container(
                    width: 150,
                    height: 150,
                    color: Colors.green,
                  ),
                  Text(
                    'Stacked Text',
                    style: TextStyle(color: Colors.white, fontSize: 20),
                  ),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}

33333333
import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('Responsive UI Example')),
        body: LayoutBuilder(
          builder: (context, constraints) {
            double width = constraints.maxWidth;

            if (width < 600) {
              // Mobile layout
              return Center(
                child: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Icon(Icons.phone_android, size: 80, color: Colors.blue),
                    Text('Mobile Layout', style: TextStyle(fontSize: 20)),
                  ],
                ),
              );
            } else {
              // Tablet / Desktop layout
              return Center(
                child: Row(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Icon(Icons.computer, size: 80, color: Colors.green),
                    SizedBox(width: 10),
                    Text('Tablet/Desktop Layout', style: TextStyle(fontSize: 20)),
                  ],
                ),
              );
            }
          },
        ),
      ),
    );
  }
}

444444444

import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      initialRoute: '/',
      routes: {
        '/': (context) => FirstScreen(),
        '/second': (context) => SecondScreen(),
      },
    );
  }
}

class FirstScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('First Screen')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              child: Text('Go to Second Screen (Normal Navigation)'),
              onPressed: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(builder: (context) {
                    return SecondScreen();
                  }),
                );
              },
            ),
            SizedBox(height: 20),
            ElevatedButton(
              child: Text('Go to Second Screen (Named Route)'),
              onPressed: () {
                Navigator.pushNamed(context, '/second');
              },
            ),
          ],
        ),
      ),
    );
  }
}

class SecondScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Second Screen')),
      body: Center(
        child: ElevatedButton(
          child: Text('Back to First Screen'),
          onPressed: () {
            Navigator.pop(context);
          },
        ),
      ),
    );
  }
}

555555555555
import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(home: CounterApp()));

class CounterApp extends StatefulWidget {
  @override
  State<CounterApp> createState() => _CounterAppState();
}

class _CounterAppState extends State<CounterApp> {
  int count = 0;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Counter App')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('Count: $count', style: TextStyle(fontSize: 25)),
            ElevatedButton(
              onPressed: () => setState(() => count++),
              child: Text('Increment'),
            ),
          ],
        ),
      ),
    );
  }
}
555555555555555
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

void main() => runApp(
  ChangeNotifierProvider(
    create: (_) => Counter(),
    child: MaterialApp(home: MyApp()),
  ),
);

class Counter extends ChangeNotifier {
  int n = 0;
  void add() { n++; notifyListeners(); }
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final c = Provider.of<Counter>(context);
    return Scaffold(
      appBar: AppBar(title: Text('Provider Demo')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('Count: ${c.n}', style: TextStyle(fontSize: 24)),
            ElevatedButton(onPressed: c.add, child: Text('Add')),
          ],
        ),
      ),
    );
  }
}


6666666666666666
import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatefulWidget {
  @override
  State<MyApp> createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  bool isDark = false;

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: isDark ? ThemeData.dark() : ThemeData.light(),
      home: Scaffold(
        appBar: AppBar(title: Text("Combined Example")),
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              MyText(),
              SizedBox(height: 20),
              MyButton(),
              SizedBox(height: 20),
              ElevatedButton(
                onPressed: () => setState(() => isDark = !isDark),
                child: Text(
                  isDark ? "Switch to Light Theme" : "Switch to Dark Theme",
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}

class MyText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Hello Flutter!",
      style: TextStyle(fontSize: 24, color: Colors.blue),
    );
  }
}

class MyButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: Text("Click Me"),
    );
  }
}

77777777777
import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      home: CombinedForm(),
      debugShowCheckedModeBanner: false,
    ));

class CombinedForm extends StatefulWidget {
  @override
  State<CombinedForm> createState() => _CombinedFormState();
}

class _CombinedFormState extends State<CombinedForm> {
  final _formKey = GlobalKey<FormState>();
  final nameController = TextEditingController();
  final emailController = TextEditingController();
  final passController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Combined Form (7a + 7b)")),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Form(
          key: _formKey,
          child: Column(
            children: [
              TextFormField(
                controller: nameController,
                decoration: InputDecoration(labelText: "Name"),
                validator: (value) =>
                    value!.isEmpty ? "Enter your name" : null,
              ),
              TextFormField(
                controller: emailController,
                decoration: InputDecoration(labelText: "Email"),
                validator: (value) =>
                    value!.contains("@") ? null : "Enter valid email",
              ),
              TextFormField(
                controller: passController,
                decoration: InputDecoration(labelText: "Password"),
                obscureText: true,
                validator: (value) =>
                    value!.length < 6 ? "Min 6 characters" : null,
              ),
              SizedBox(height: 20),
              ElevatedButton(
                onPressed: () {
                  if (_formKey.currentState!.validate()) {
                    print("Name: ${nameController.text}");
                    print("Email: ${emailController.text}");
                    print("Password: ${passController.text}");
                    ScaffoldMessenger.of(context).showSnackBar(
                      SnackBar(content: Text("Form Submitted")),
                    );
                  }
                },
                child: Text("Submit"),
              ),
            ],
          ),
        ),
      ),
    );
  }
}

88888888888
import 'package:flutter/material.dart';
void main() => runApp(MaterialApp(
      home: CombinedAnimation(),
      debugShowCheckedModeBanner: false,
    ));
class CombinedAnimation extends StatefulWidget {
  @override
  State<CombinedAnimation> createState() => _CombinedAnimationState();}
class _CombinedAnimationState extends State<CombinedAnimation> {
  double size = 100;
  bool change = false;
 @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Combined Animation (8a + 8b)")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            AnimatedContainer(
              width: size,
              height: size,
              duration: Duration(seconds: 1),
              color: Colors.blue, ),
            SizedBox(height: 20),
            AnimatedOpacity(
              opacity: change ? 0.2 : 1.0,
              duration: Duration(seconds: 1),
              child: Container(width: 100, height: 100, color: Colors.red),   ),
            SizedBox(height: 20),
            AnimatedContainer(
              width: 100,
              height: 100,
              color: Colors.green,
              duration: Duration(seconds: 1),
              margin: EdgeInsets.only(left: change ? 120 : 0),),
            SizedBox(height: 25),
            ElevatedButton(
              onPressed: () {
                setState(() {
                  size = size == 100 ? 200 : 100;
                  change = !change;
                });  },
              child: Text("Animate"), ), ], ), ),);}}
99999999
10101010
import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(home: MyHome()));

class MyHome extends StatefulWidget {
  @override
  State<MyHome> createState() => _MyHomeState();
}

class _MyHomeState extends State<MyHome> {
  String message = "No message yet";

  void showMessage() {
    setState(() => message = "Hello Debugger!");
    debugPrint("Message updated: $message");
  }

  @override
  Widget build(BuildContext context) {
    debugPrint("Building Widget... $message");
    return Scaffold(
      appBar: AppBar(title: Text("Unit Test & Debug Example")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              key: Key("myButton"),
              onPressed: () {},
              child: Text("Test Button"),
            ),
            SizedBox(height: 20),
            Text(message, style: TextStyle(fontSize: 20)),
            SizedBox(height: 10),
            ElevatedButton(
              onPressed: showMessage,
              child: Text("Show Message"),
            ),
          ],
        ),
      ),
    );
  }
}

